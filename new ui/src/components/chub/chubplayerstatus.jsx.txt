import React from "react";
import CHubProgressBar from "./CHubProgressBar";
import CHubCurrency from "./CHubCurrency";
import CHubStatusIndicator from "./CHubStatusIndicator";
import CHubRankBadge from "./CHubRankBadge";

export default function CHubPlayerStatus({ name, level, gs, xp, xpMax, coins, status = "online", rank }) {
  return (
    <div className="flex items-center gap-5 border border-[#232A33] bg-gradient-to-b from-[#151A21] to-[#0F1318] px-4 py-3">
      <div className="relative flex h-9 w-9 items-center justify-center border border-[#3A2F17] bg-[#15120C] font-heading text-[13px] font-bold text-[#F0C25A]">
        {name.slice(0, 1).toUpperCase()}
        <span className="absolute -bottom-0.5 -right-0.5 ring-1 ring-[#0F1318]">
          <CHubStatusIndicator status={status} />
        </span>
      </div>
      <div className="min-w-0">
        <div className="flex items-center gap-2">
          <span className="font-heading text-[14px] font-semibold tracking-wide text-[#F0C25A]">{name}</span>
          {rank && <CHubRankBadge rank={rank} />}
        </div>
        <div className="mt-0.5 flex items-center gap-2 text-[10px] uppercase tracking-[0.16em] text-[#6E7883]">
          <CHubStatusIndicator status={status} showLabel />
          <span className="h-2 w-[1px] bg-[#2A313B]" />
          <span>LVL {level}</span>
          <span className="h-2 w-[1px] bg-[#2A313B]" />
          <span>GS {gs}</span>
        </div>
      </div>
      <div className="flex-1">
        <div className="mb-1.5 flex items-center justify-between text-[9px] uppercase tracking-[0.22em] text-[#5A636E]">
          <span>XP</span>
          <span className="text-[#8A939E]">{xp} / {xpMax}</span>
        </div>
        <CHubProgressBar value={xp} max={xpMax} />
      </div>
      <CHubCurrency amount={coins} />
    </div>
  );
}